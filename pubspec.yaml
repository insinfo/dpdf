name: pdfcraft
version: 1.0.0

environment:
  sdk: ^3.6.0

dev_dependencies:
  test: ^1.26.3
  # Independent test oracle; never imported by the runtime library.
  crypto: ^3.0.7
  pointycastle: ^4.0.0
