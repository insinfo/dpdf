/// The possible values for the type of overflow of block elements.
enum OverflowPropertyValue {
  /// Fit content within bounds.
  fit,

  /// Content is visible even if it overflows.
  visible,

  /// Content is hidden when it overflows.
  hidden,
}
